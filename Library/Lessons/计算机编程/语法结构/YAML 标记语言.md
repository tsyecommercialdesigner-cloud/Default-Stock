---
title: YAML 标记语言
created: 2026-08-06
tags:
  - YAML
---
---

# 基本介绍

YAML 语言首次发表于 2001 年，比 XML 的诞生时间晚了 3 年。YAML 虽然在名字上和 XML 相似，但实质上与 XML 完全不同，更适合人类阅读，计算机解析起来也更容易[^1]。

[^1]: YAML 是“YAML Ain't a Markup Language”的缩写，类似 PHP 的递归形式，但它早期真正的名字是“Yet Another Markup Language”。它的发音可能让人觉得有点怪，可以参考“yard”和“camel”这两个词的发音。

YAML 的官网上有对语言规范的完整介绍，这里不再列举语言的细节，只讲与 Kubernetes 相关的要点，帮助大家快速掌握 YAML。

YAML 支持整数、浮点数、布尔、字符串、数组和对象等数据类型，是 JSON 的超集，也就是说，任何合法的 JSON 文档也是 YAML 文档。如果读者了解 JSON，那么学习 YAML 会很容易[^2]。

[^2]: 推荐一个知名的 JSON/YAML 工具网站：BEJSON，支持 JSON 格式校验，也可以把 JSON 格式转换为 YAML。

和 JSON 相比，YAML 的语法更简单、形式也更清晰紧凑，例如：

- 使用空白与缩进表示层次（类似 Python），可以不使用花括号和方括号；
- 支持使用“`#`”书写注释，比起 JSON 是很大的改进；
- 对象（字典）的格式与 JSON 基本相同，但 Key 不需要使用双引号；
- 数组（列表）是使用“`-`”开头的清单形式（类似 Markdown）；
- 表示对象的“`:`”和表示数组的“`-`”后面都必须有空格；
- 可以使用“`---`”在一个文件里分隔多个 YAML 对象。

---

# 示例

下面是 3 个 YAML 的简单示例。

## 示例 1：数组，使用“-”列出了 3 种操作系统。

```yaml
# YAML 数组(列表)
OS:
  - Linux
  - macOS
  - Windows
```

它等价的 JSON 如下：

```json
{
  "OS": ["Linux", "macOS", "Windows"]
}
```

对比可以看到 YAML 在形式上很简单，避免了闭合花括号、方括号的麻烦，每个元素后面不需要逗号。

## 示例 2：YAML 对象，声明了一些计算节点。

```yaml
# YAML 对象(字典)
Kubernetes:
  master: 1
  worker: 3
```

它等价的 JSON 如下：

```json
{
  "Kubernetes": {
    "master": 1,
    "worker": 3
  }
}
```

注意 YAML 里的 Key 不需要使用双引号，看起来更舒服。

## 示例 3：组合 YAML 的数组和对象，可以描述任意的 Kubernetes 资源对象。

```yaml
# 复杂的例子，组合数组和对象
Kubernetes:
  master:
    - apiserver: running
    - etcd: running
  node:
    - kubelet: running
    - kube-proxy: down
    - container-runtime: [docker, Containerd, cri-o]
```

---

# 其它知识

关于 YAML 语言的其他知识如下所示，读者可以参考 YAML 官网学习。


<p align=center>YAML语言的语法细节</p>

| 基本语法                                                                                 | 基本类型                                                                                                                                                                                                         | 复合类型                                                                                                                                     |
| :----------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------- |
| • 是 JSON 的超集<br>• 缩进对齐表示层次<br>• 使用“#”书写注释<br>• “:”、“-”后必须有空格<br>• `---` 分隔多个 YAML 对象 | • **布尔值**：`true` / `false`<br>• **数字**：整数 / 浮点数<br>• **字符串**：<br>&nbsp;&nbsp;- 不强制使用引号<br>&nbsp;&nbsp;- 双引号里使用 `\` 转义<br>&nbsp;&nbsp;- 多行使用 “\|”：“\|- ”去掉最后的换行，“\|+ ”保留最后的换行<br>&nbsp;&nbsp;- 多行使用 `>`：合并成一行 | • **数组（列表）**：<br>&nbsp;&nbsp;- 使用 `-`<br>&nbsp;&nbsp;- `[a, b, c]`<br>• **对象（字典）**：<br>&nbsp;&nbsp;- 使用 `:`<br>&nbsp;&nbsp;- `{a, b, c}` |
