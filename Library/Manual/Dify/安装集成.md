
运行：

```
cd /opt/dify/docker
grep FORCE_VERIFYING_SIGNATURE .env
```

检查修改的配置项是否已生效：

```
FORCE_VERIFYING_SIGNATURE=false
```

然后重建插件相关容器：

```
docker compose -p dify up -d --force-recreate plugin_daemon api worker
```

可选看日志：

```
docker compose -p dify logs -f plugin_daemon
```
